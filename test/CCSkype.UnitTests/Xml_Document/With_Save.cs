using NUnit.Framework;

namespace CCSkype.UnitTests.Xml_Document
{
    // ReSharper disable InconsistentNaming
    [TestFixture]
    public class With_Save
    {
        [SetUp]
        public void SetUp()
        {
        }

        [TearDown]
        public void TearDown()
        {
        }

        [Test]
        public void Should_Save()
        {
            //var file = Guid.NewGuid() + ".xml";
            //var xml = "<xml />";
            //IXmlDocument xmlDocument = new XmlDocumentImpl(xml);
            //xmlDocument.Save(file);
            //Assert.That(File.ReadAllText(file), Is.EqualTo(xml));
            //File.Delete(file);
        }
    }
}